// Legacy import path. Canonical definitions live in `src/shared/types`.
// Kept as a re-export so existing `@/types` imports continue to work
// through Phase 1; later phases will migrate call sites directly.
export * from '@/shared/types';
